<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD</title>
    <link rel="stylesheet" href="stylesheets/stylesheet.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
   <div class="w-full h-full bg-zinc-900 p-10 text-white">
    <h1 class="mb-3">Edit User</h1>
    <div><a href="/read">See all users</a></div>
    <form action="/update/<%= user._id %>" method="post">
        <input class="px-5 py-3 py-2 bg-transparent border-2 border-zinc-800 rounded-lg outline-none " type="text" name="name" value="<%= user.name %>" placeholder="Name">
        <input class="px-5 py-3 py-2 bg-transparent border-2 border-zinc-800 rounded-lg outline-none " type="text" name="email" value="<%= user.email %>" placeholder="email">
        <input class="px-5 py-3 py-2 bg-transparent border-2 border-zinc-800 rounded-lg outline-none " type="text" name="image" value="<%= user.image %>" placeholder="image URL">
        <input class="px-5 py-3 py-2 ml-4 bg-yellow-500 border-2 border-zinc-800 rounded-lg" type="submit" value="update">

    </form>
   </div>
</body>
</html>